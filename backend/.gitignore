# Secrets — NEVER commit
.env
.env.local
*.key
wallet*.json

# Python
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
*.egg-info/

# Runtime data (track record + caches) — ignore everything except templates
data/*
!data/*.example.json

# Swarms framework scratch dir
agent_workspace/
